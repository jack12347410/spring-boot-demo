spring:
  profiles:
    active: dev
  thymeleaf:
    mode: HTML

server:
  servlet:
    context-path: /jack/v1
book:
  Name: Who are yo
  Author: Jack
  Id: ${random.uuid}
  Description: ${book.name}, very good.
